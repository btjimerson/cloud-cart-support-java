spring:
  datasource:
    url: jdbc:postgresql://postgres.cloud-cart.svc:5432/customers
    driver-class-name: org.postgresql.Driver
    username: customers_user
    password: ${CUSTOMERS_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
