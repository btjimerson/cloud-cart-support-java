spring:
  datasource:
    url: jdbc:postgresql://postgres.cloud-cart.svc:5432/notifications
    driver-class-name: org.postgresql.Driver
    username: notifications_user
    password: ${NOTIFICATIONS_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
