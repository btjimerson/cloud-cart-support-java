apiVersion: gateway.kgateway.dev/v1alpha1
kind: HTTPListenerPolicy
metadata:
  name: websocket-upgrade
  namespace: kgateway-system
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: cloud-cart-gateway
  upgradeConfig:
    enabledUpgrades:
      - websocket
